## Useful commands

### foreach

array.foreach(element => {
  console.log(element);
})

### map

array.map(element => {
  return element;
})

### Javascript General

let name = "     George Lympe     ";

name.trim() = "George Lympe"
